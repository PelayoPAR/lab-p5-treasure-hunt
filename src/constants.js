const WIDTH = 1000;
const HEIGHT = WIDTH;
const SQUARE_SIDE = WIDTH / 10;
const RIGHT_ARROW = 39;
const DOWN_ARROW = 40;
const LEFT_ARROW = 37;
const UP_ARROW = 38;
const matX = 9;
const matY = 9;

const vikingDirections = []

// Grid Lines
// let y = 0
// let x = 0
// const horizontalLine = line(0, y, WIDTH, y)
// const verticalLine = line(x, 0, x, HEIGHT)

// line(0, 0, WIDTH, 0); // horizontal top border
//     line(0, 0, 0, HEIGHT); // vertical left border
//     line(WIDTH, HEIGHT, WIDTH, 0); //horizontal bottom border
//     line(0, HEIGHT, WIDTH, HEIGHT); //vertical right border
